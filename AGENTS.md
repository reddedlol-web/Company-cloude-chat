# Company-cloude-chat — инструкции для AI-агента

Краткий паспорт проекта. Источник правды — файлы репозитория (README, docs/, код), не догадки.

## Что это

- **Путь:** `Company-cloude-chat`
- **Стек:** unknown


## Перед работой

1. Прочитай `README.md` (если есть).
2. Если есть `docs/` — открой индекс/README внутри docs.
3. Соблюдай существующие `.cursor/rules/*` в этом репо.

## Не делать

- Не коммитить `.env`, токены, ключи.
- Не `git push --force` в `main`/`master` без явной просьбы пользователя.
- Не переписывать архитектуру «заодно», если задача точечная.

## Проверки

- Следуй скриптам из README / package.json / pyproject, если есть.

## Заметки cursor-ops

Этот файл создан/обновлён из `~/Projects/cursor-ops`. Существующие сильные AGENTS.md не затираются автоматически.
